services:
  - type: web
    name: nk-hamster
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: HOSTNAME=0.0.0.0 node .next/standalone/server.js
    envVars:
      - key: NODE_ENV
        value: production
